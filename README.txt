A program made for a client, this repo has mock data. To run the program, use python 3+ 
with the dependencies and packages, run Forecast24042019_gui.py, then first select which 
data file you want to read, the next prompt would be for the configs. Select the excel 
file named config, and the GUI would run with all the functionalities. 
